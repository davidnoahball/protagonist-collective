<div body-wrap>
  <div class="list-group">
    <a href="/adventures/{{adventure.id}}/pages/{{adventure.root}}" class="list-group-item" ng-repeat="adventure in adventures">
      {{adventure.title}}
      <span class="badge">{{adventure.page_count}} / {{adventure.critical_size}}</span>
    </a>
  </div>
</div>

<% 
=begin %>

Have to add checkboxes for complete critical non-critical
Can do pass an attribute through the api

<a href=<%= adventure.root_path %> class="list-group-item">
  <%= adventure.title %>
  <span class="badge"><%= adventure.page_count %> / <%= adventure.critical_size %></span>
</a>

<div class="container body-drop" style="height:100%">
  <div class="jumbotron" style="border-style:solid; border-color:black; border-width:3px; min-height:calc(100% - 200px); position:relative">

<div class="list-group">
  <%= render(Adventure.all) %>
</div>

  </div>
  <div>
    <% 2.times do %>
      <br>
    <% end %>
  </div>
</div>



<% 
=end %>