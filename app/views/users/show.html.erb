<%= @user.name %>
<% if session[:user_id] == @user.id %>
  <%= form_for :user, url: "/logout" do |f| %>
    <%= f.submit "Logout" %>
  <% end %>
<% end %>
<br><br>
<%= render(@user.pages) || "No pages written yet!" %>